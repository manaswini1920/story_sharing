<%-include('../partials/header.ejs')%>
    <div class="content">
                    <p><%=story.title %></p>
                    <p><%=story.content %></p>
                    <p><%=story.author%></p>
                    <p><%=story.createdAt%></p>
        
    </div>
    <form>
        <button type="submit" formaction="/stories/<%=story._id %>/edit" formmethod="get">Edit</button>
        <button type="submit" formaction="/stories/<%=story._id%>?_method=DELETE" formmethod="post">Delete</button>

    </form>
    <%-include('../partials/footer.ejs')%>